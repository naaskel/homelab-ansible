- name: Update system packages
  hosts: homelab
  become: true

  tasks:
    - name: Update apt cache and upgrade packages
      apt:
        update_cache: true
        upgrade: dist

    - name: Remove unused packages
      apt:
        autoremove: true
        purge: true
